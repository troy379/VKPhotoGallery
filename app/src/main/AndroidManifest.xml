<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.stfalcon.vkphotogallery">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".features.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".features.auth.AuthActivity"/>
        <activity
            android:name=".features.places.autocomplete.PlacesAutocompleteActivity"/>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDYyvEC3qb0KUxHax-YK5r1DYI7rntvIyg"/>

    </application>

</manifest>